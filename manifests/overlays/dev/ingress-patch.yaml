apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: samplejava-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: samplejava.edu24-dev.20.249.165.109.nip.io
    http:
      paths:
      - path: /
        backend:
          service:
            name: samplejava-svc
            port:
              number: 80
        pathType: Prefix